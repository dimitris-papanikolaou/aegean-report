# NOTE: this file is INERT for a GitHub Pages *project* site. Crawlers read
# robots.txt only at the domain root (username.github.io/robots.txt), never at
# a project subpath, so nothing here is honoured. The `noindex` meta tags in
# index.html are what actually keep the page out of search results. This file
# is kept only so the rule travels with the repo if it is ever served from a
# domain root — do not treat its presence as protection.
User-agent: *
Disallow: /